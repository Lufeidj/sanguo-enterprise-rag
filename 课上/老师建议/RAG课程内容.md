# RAG课程内容

## 一、RAG的介绍，和传统数据库的区别

RAC
检索增强生成
## 检索
- 从哪里检索 DB

VDB
Milvus(市面上有很多)理解为 mysql

- 向量数据库

数据 --- >向量

Embedding模型 (文本、音频、视频)

为什么要转化成 ** 向量 **? 提供一个核心的能力 ** 相似度匹配 ** (听歌识曲,以图搜图)

文本数据的存储 通常涉及到一个行为 :** 切片 **
-固定规则切片、滑动窗口切片、AI辅助切片、切片概要生成

(为什么要滑动窗口去切割?防止语义割裂)

- 向量库表 维度(726,1024)

建表的时候也要设置这个维度 (一般认为向量特征越大 细节会越丰富 相似度匹配会更精准)

(缺乏可解释性)

-索引:

暴力检索 低于10万 HNSW 图检索

- 字段:
向量字段
有哪些检索方式
- 标量检索 mysql
- 向量检索 基于语义 去找相似度比较高的切片 4
- 关键字检索 BM25 tf-idf 不需要用到 EMBEDDING 模型
- 混合检索 (向量检索+关键字检索)
-两个重要的参数:

Top K

Score

## 增强生成
-把检索到的内容放进提示词上下文 用于 ** 增强 LLM 生成 **
markdown

##以下是知识库检索内容,请严格按照里面的描述进行回答:
{{检索内容}}

- RAG 标注增强



## Milvus的安装

冷热数据 以及 内存和硬盘的区别
collection 操作前需要先被load
db 中会有一张一模一样的表 只是没有向量字段 (备份作用，随时全量同步,
重复数据对VDB的干扰是巨大的，参考TOP)id
Emebedding 模型 调用 参考百炼平台